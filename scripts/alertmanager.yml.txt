global:
  smtp_smarthost: 'localhost:25'
  smtp_from: 'alertes@mediaschool.local'
  smtp_require_tls: false

route:
  receiver: 'mail-alerts'
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 5m

receivers:
  - name: 'mail-alerts'
    email_configs:
      - to: 'destinataire@example.com'
